<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto" xmlns:tools="http://schemas.android.com/tools"
              android:layout_width="match_parent"
              android:layout_height="match_parent" android:orientation="vertical" android:gravity="center"
>

    <ImageView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:id="@+id/fImage"
            android:scaleType="centerCrop"
            tools:srcCompat="@drawable/jpg1" android:layout_weight="8"/>

    <RatingBar
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:numStars="5"
            android:id="@+id/ratingBar" android:layout_weight="2" android:paddingTop="12dp"/>
</LinearLayout>
